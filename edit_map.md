下面根据你提供的截图，我整理了一份**可以直接用于生成前端页面的 UI / 交互设计文档（PRD级别）**。该文档结构已经按 **前端实现 + 组件结构 + 交互逻辑**拆分，适合用 **React + Ant Design + Konva / Canvas** 生成页面。

---

# 质检机器人系统 — 地图编辑页面 UI / 交互设计文档

页面名称：地图编辑器
页面路径：监控 → 地图编辑
页面作用：用于配置机器人运行地图，包括 **路径点、工作点、充电点、区域、路径连接等**

---

# 一、页面整体布局

页面采用 **三栏式结构**

```
--------------------------------------------------------
顶部导航栏
--------------------------------------------------------
| 左侧工具栏 |           地图编辑画布          | 属性栏 |
|            |                                |        |
|            |                                |        |
|            |                                |        |
--------------------------------------------------------
```

页面宽度：100%
页面高度：100vh

布局比例：

| 区域    | 宽度    |
| ----- | ----- |
| 左侧工具栏 | 60px  |
| 地图编辑区 | 自适应   |
| 右侧属性栏 | 300px |

---

# 二、顶部导航栏

高度：60px
背景色：深蓝色

元素：

| 元素        | 说明     |
| --------- | ------ |
| Robots OS | 系统Logo |
| 监控        | 当前选中菜单 |
| 运维        | 跳转运维页面 |
| 统计        | 跳转统计页面 |
| 设置        | 系统设置   |
| Admin     | 用户信息   |

交互：

* 点击 **监控 / 运维 / 统计** 切换页面
* 当前页面高亮显示

---

# 三、左侧工具栏（地图编辑工具）

位置：页面左侧
宽度：60px

工具按功能分组

---

# 1 操作工具

图标：

| 功能 | 说明      |
| -- | ------- |
| 撤销 | 撤销上一步操作 |
| 重做 | 恢复撤销操作  |

交互：

点击撤销：

```
恢复到上一步地图状态
```

实现：

需要维护操作栈

```
undoStack
redoStack
```

---

# 2 选择工具

工具名称：选中

图标：手型图标

功能：

用于选择地图元素

支持：

* 单选节点
* 框选节点
* 多选节点

交互：

点击后进入选择模式

鼠标操作：

| 操作   | 结果     |
| ---- | ------ |
| 点击节点 | 选中节点   |
| 拖动鼠标 | 框选多个节点 |

选中效果：

```
节点变蓝色
```

---

# 3 节点创建工具

用于创建地图点位

节点类型：

| 类型  | 说明       |
| --- | -------- |
| 路径点 | 普通导航点    |
| 工作点 | 机器人执行任务点 |
| 充电点 | 机器人充电位置  |
| 空闲点 | 机器人待机点   |

操作流程：

```
1 选择节点类型
2 点击地图
3 生成节点
```

节点UI：

```
灰色方块
```

节点尺寸：

```
12px × 12px
```

节点数据结构

```
{
 id:"node001",
 type:"waypoint",
 x:100,
 y:200
}
```

---

# 4 区域绘制工具

用于绘制区域

类型：

| 工具  | 说明      |
| --- | ------- |
| 矩形  | 绘制矩形区域  |
| 多边形 | 绘制多边形区域 |

区域用途：

* 禁行区域
* 工作区域
* 设备区域

操作：

矩形：

```
鼠标拖动生成
```

多边形：

```
逐点点击生成
双击结束
```

数据结构：

```
{
 id:"area001",
 type:"polygon",
 points:[]
}
```

---

# 5 对齐工具

用于对齐多个节点

功能：

| 功能   | 说明     |
| ---- | ------ |
| 左对齐  | 节点左侧对齐 |
| 右对齐  | 节点右侧对齐 |
| 上对齐  | 节点顶部对齐 |
| 下对齐  | 节点底部对齐 |
| 垂直居中 | 垂直居中   |
| 水平居中 | 水平居中   |

交互：

```
1 选中多个节点
2 点击对齐按钮
```

---

# 四、右键菜单（地图操作）

当在地图上右键点击时弹出菜单

菜单内容：

| 菜单   | 功能     |
| ---- | ------ |
| 新增点位 | 创建节点   |
| 新增路径 | 创建路径   |
| 新增区域 | 创建区域   |
| 删除选中 | 删除当前选中 |
| 录制点位 | 自动记录路径 |
| 拉直路径 | 自动调整路径 |
| 画布重置 | 重置画布   |
| 画布旋转 | 旋转地图   |
| 画布全屏 | 全屏显示   |

---

# 1 新增路径

功能：

连接两个节点

操作：

```
点击节点A
点击节点B
生成路径
```

路径UI：

```
黑色线段
```

数据结构：

```
{
 id:"edge001",
 from:"node001",
 to:"node002"
}
```

---

# 2 删除选中

删除：

```
节点
路径
区域
```

操作：

```
右键 → 删除选中
```

---

# 五、地图编辑区域（Canvas）

页面中心区域

功能：

用于显示和编辑地图

显示元素：

| 类型  | UI     |
| --- | ------ |
| 节点  | 小方块    |
| 路径  | 线      |
| 区域  | 半透明多边形 |
| 站点  | 蓝色矩形   |
| 障碍区 | 红色矩形   |

截图示例：

```
质检站1
质检站2
```

虚拟墙：

```
红色矩形
```

---

# 六、地图画布功能

支持操作：

| 操作   | 说明   |
| ---- | ---- |
| 拖动画布 | 平移地图 |
| 鼠标滚轮 | 缩放   |
| 点击节点 | 选择节点 |
| 拖动节点 | 移动节点 |

缩放范围：

```
0.5x - 3x
```

---

# 七、右侧属性面板

位置：

页面右侧

宽度：

```
300px
```

模块名称：

```
地图属性
```

字段：

| 字段   | 类型  |
| ---- | --- |
| 地图编码 | 输入框 |
| 地图名称 | 输入框 |
| 编辑时间 | 只读  |
| 发布时间 | 只读  |

示例：

```
地图编码: map001
地图名称: 一楼地图
编辑时间: 2026/3/5
发布时间: 2026/3/5
```

数据结构：

```
{
 mapId:"map001",
 mapName:"一楼地图",
 editTime:"",
 publishTime:""
}
```

---

# 八、地图数据结构设计

完整地图数据：

```
{
 nodes:[],
 edges:[],
 areas:[]
}
```

节点

```
nodes:[
 {
   id:"node001",
   type:"waypoint",
   x:100,
   y:200
 }
]
```

路径

```
edges:[
 {
   id:"edge001",
   from:"node001",
   to:"node002"
 }
]
```

区域

```
areas:[
 {
  id:"area001",
  type:"polygon",
  points:[]
 }
]
```

---

# 九、前端组件结构（React）

建议组件结构

```
MapEditorPage
 ├── TopNavbar
 ├── MapToolbar
 ├── MapCanvas
 ├── PropertyPanel
 └── ContextMenu
```

核心组件

MapCanvas

负责：

```
节点绘制
路径绘制
区域绘制
拖拽
缩放
框选
```

推荐技术：

```
React
Ant Design
react-konva
```

---

# 十、地图编辑核心能力

地图编辑器必须支持

```
节点创建
节点删除
节点拖拽
节点框选
路径创建
路径删除
区域绘制
区域编辑
画布缩放
画布平移
撤销
重做
```

---

如果你需要，我可以再帮你 **直接生成一套完整可运行的前端代码**：

包括：

* React地图编辑器
* Konva地图绘制
* 节点拖拽
* 路径连接
* 框选
* 右键菜单

代码大概 **500~800行，可以直接跑起来的机器人地图编辑系统原型**。
