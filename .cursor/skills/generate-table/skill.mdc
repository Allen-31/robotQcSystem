---
name: generate-table
description: 生成带 TypeScript 类型的 Ant Design 表格列配置
---

为 Ant Design Table 生成 **带类型的列配置**。

- 使用 `antd/es/table` 的 `ColumnsType<T>`，T 为行数据类型。
- 每列包含：`title`、`dataIndex`、`key`（或由 dataIndex 推导）。
- Table 上设置 `rowKey`（如 `rowKey="id"`）。
- 日期列可用 `render` 配合 dayjs/format。
- 枚举/标签列用 `render` 配合 Tag 或选项文案。
- 操作列：`render: (_, record) => <Space>...</Space>`，内含编辑/删除等。

示例：

```tsx
import type { ColumnsType } from 'antd/es/table';

const columns: ColumnsType<User> = [
  { title: 'ID', dataIndex: 'id', key: 'id', width: 80 },
  { title: '姓名', dataIndex: 'name', key: 'name' },
  { title: '邮箱', dataIndex: 'email', key: 'email' },
  {
    title: '创建时间',
    dataIndex: 'createdAt',
    key: 'createdAt',
    render: (val: string) => dayjs(val).format('YYYY-MM-DD HH:mm'),
  },
  { title: '操作', key: 'action', width: 120, render: (_, record) => <ActionButtons record={record} /> },
];
```

Table 用法：使用外部 Pagination 时设为 `rowKey="id"`、`columns={columns}`、`dataSource={data}`、`pagination={false}`。
