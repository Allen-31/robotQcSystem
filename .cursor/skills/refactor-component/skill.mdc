---
name: refactor-component
description: 将过大的 React 组件拆成更小的组件与 hooks
---

当组件过大（超过 300 行）或逻辑过多时：

1. **拆出子组件**
   - 将一块 JSX 抽成新组件，放在同文件或 `components/` 子目录。
   - 只传必要 props，保持接口精简。

2. **拆出自定义 hooks**
   - 将状态与副作用（请求、表单提交、弹窗开关）抽到 `useXxx` hook。
   - Hook 返回状态与处理函数，页面组件只做薄视图。

3. **使用记忆化**
   - 若子组件接收的 props 较稳定，用 `React.memo` 包裹。
   - 派生数据用 `useMemo`，传给子组件的回调用 `useCallback`。

4. **控制文件行数**
   - 组件文件 < 300 行，hook < 200 行，工具 < 150 行。

仅调整结构与文件大小，不改变行为。
