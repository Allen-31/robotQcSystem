---
name: create-store
description: 生成 Zustand store
---

在 **src/store**（或按功能划分时的功能目录下）创建 Zustand store。

- 使用 `zustand` 的 `create`。
- Hook 命名为 `use{Entity}Store`。
- 状态与动作保持清晰；不要把整页 CRUD 逻辑塞进 store（页面级状态优先用 hooks）。

示例：

```ts
import { create } from 'zustand';

interface User {
  id: number;
  name: string;
  email: string;
}

interface UserState {
  user: User | null;
  setUser: (user: User | null) => void;
}

export const useUserStore = create<UserState>((set) => ({
  user: null,
  setUser: (user) => set({ user }),
}));
```

列表/分页建议放在页面级 hooks（如 useUserPage）中调 api；store 用于全局用户/会话或共享 UI 状态。
